# tools/caum_sync_witness.rish — the desk pull loop: fetch the whole of what a far hand holds in one bounded op (CAUM12).
#
# Season A (Hardware & Right-to-Repair) · waymark CAUM · Mikrophone firmware journey · rung CAUM12.
# Design read: active-designing/20260814-071500_caum12-desk-sync-pull-loop-exploration.md
# Stands on: mikrophone/manifest.rye (CAUM8), mikrophone/serve.rye (CAUM9), mikrophone/catalog.rye (CAUM7).
#
# mikrophone/sync.rye lifts CAUM9's inline convergence into one named, reusable, bounded operation: pull reads a remote
# desk's manifest verify-before-trust, then for each advertised address the local desk lacks, serves it and deposits it,
# ending with the local desk holding every address the remote advertised. The selftest proves: a fresh desk converges
# whole, a partial desk pulls only its lack, a second pull is idempotent, a tampered advertisement refuses DigestMismatch
# before any frame crosses, and a desk at its entry bound refuses CatalogFull by name.
#
# Run from the repository root:
#   rishi/bin/rishi run tools/caum_sync_witness.rish

say "caum-sync: Language EN — CAUM12 lifts the local content-addressed sync into one bounded operation; pull fetches every advertised recording a desk lacks, asking only for its lack."
say "caum-sync: Style Radiant — the desk learns to fetch the whole of what a far hand holds, asking only for what it lacks and proving every byte before it lands, so two hands meet on the same whole."
say "caum-sync: Lens TAME — pull verifies the manifest before reading a record, serves only the wanted through the one proven framing path, asserts convergence (holds every advertised address) and served == wanted; a tampered advertisement refuses DigestMismatch and a full desk refuses CatalogFull; pulling over a real transport stays the Comlink-served gate; no disk, no network, no key, no funds."

let zig = "vendor/zig-toolchain/zig"
let bin = "mikrophone/bin/sync"

let build = run ["sh" "-c" "mkdir -p mikrophone/bin && env RYE_ZIG=${zig} rye/bin/rye build mikrophone/sync.rye -femit-bin=${bin}"]
assert build.ok else "caum-sync: build failed — ${build.err}"

let outfile = "mikrophone/bin/sync.out"
let selftest = run ["sh" "-c" "${bin} selftest > ${outfile} 2>&1"]
assert selftest.ok else "caum-sync: selftest exited non-zero — see ${outfile}"

let shown = run ["sh" "-c" "cat ${outfile}"]
say "caum-sync: ${shown.out}"

let green = run ["sh" "-c" "grep -q 'GREEN caum-sync' ${outfile} && echo yes"]
assert (trim green.out) == "yes" else "caum-sync: selftest did not print the GREEN caum-sync line"

say "caum-sync: GREEN — the Mikrophone desk pulls the whole of what a far hand holds in one bounded operation. CAUM12 makes the git pull a reusable primitive; pulling over a real transport waits for the Comlink-served gate."
